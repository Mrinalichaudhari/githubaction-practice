name: cicdthree
on:
  workflow_dispatch: 
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo "need to clone the code"
      
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build the code
        run : echo "build code"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test the code
        run : echo "test the code"
  deploy:
      runs-on: ubuntu-latest
      steps:
        - name: deploy the code
          run : echo "deploy the code"
        
