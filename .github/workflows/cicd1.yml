name: cicdone
on: workflow_dispatch

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo "code clone"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build the code
        run: echo "build the code"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test the code
        run: echo "test the code"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: seplloy the code
        run: echo "deploy the code"
